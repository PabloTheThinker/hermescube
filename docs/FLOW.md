# Day-to-day flow (low friction)

1. `hermescube connect` once per home  
2. Agent turns: provider prefetches **clean** recall (surface filter)  
3. Writes batch every 10 assistant turns + session end  
4. `hermescube query` / search for doctrine, not DOT spam  
5. Weekly: `hermescube hygiene` · `hermescube security audit` · `hermescube blackbox hold`  
6. Risk: `checkpoint create` · crash: `handoff` + blackbox line  

Prefetch hides: superseded, `[DOT]`, test handoffs, PERSIST-PROOF, low-trust junk.
